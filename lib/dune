(library
 (name qunity_prototypes)
 (modules util reals syntax typechecking matrix semantics parser lexer))

(menhir
 (modules parser))

(ocamllex lexer)

(env
 (dev
  (flags
   (:standard -warn-error -A))))
