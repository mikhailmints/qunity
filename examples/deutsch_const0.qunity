def Deutsch <F> :=
    let x {Bit} = Bit0 |> Had in (
        ctrl {Bit, Bit} (F of x) [
            Bit0 -> x;
            Bit1 -> x |> gphase {Bit, pi}
        ]
    ) |> Had
end

def Const0 <T> :=
    lambda x {T} -> Bit0
end

Deutsch <Const0 <Bit>>
