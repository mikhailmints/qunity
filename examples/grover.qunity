def #N := 5 end
def $Answer : Num{#N} := ($0, ($1, ($1, ($0, ($0, ()))))) end

/*
Quantum oracle representing a function acting on N bits that outputs
1 for the input Answer and 0 for other inputs
*/
def @F : Num{#N} -> Bit :=
    lambda x -> ctrl x [
        $Answer -> (x, $1);
        else -> (x, $0)
    ] |> @Snd{Num{#N}, Bit}
end

$Grover{Num{#N}, $NTensorE{#N, Bit, $Plus}, @F, 1}
